<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Темная тема приложения -->
    <style name="Theme.ZmeyGorynych" parent="Theme.Material3.DayNight">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background colors -->
        <!-- УДАЛИТЬ ЭТУ СТРОКУ: <item name="colorBackground">@color/black</item> -->
        <item name="colorOnBackground">@color/white</item>

        <!-- Использовать colorSurface для фона -->
        <item name="colorSurface">@color/black</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorOnSurfaceVariant">@color/white</item>

        <!-- Добавить для фона окна -->
        <item name="android:windowBackground">@color/black</item>

        <!-- Error color -->
        <item name="colorError">#FFCF6679</item>

        <!-- Также добавьте остальные необходимые атрибуты из светлой темы -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
    </style>
</resources>